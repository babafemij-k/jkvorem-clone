import React from "react";

const EmailInput = () => {
  return (
    <input
      type="email"
      name="email"
      placeholder="Email Address"
      style={{ marginBottom: "0.5rem" }}
    />
  );
};

export default EmailInput;
